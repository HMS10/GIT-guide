# Git

# Initialize Git project:
  git init

# Add all files to staging area:
  git add -A

# Add all files to staging area:
  git checkout -- .

# Clone project:
  git clone REPO_WEBSITE

# Push destination:
  git remote -v

# Push to remote repository:
  git push origin master 

  git remote add origin REPO_WEBSITE
  git remote set-url